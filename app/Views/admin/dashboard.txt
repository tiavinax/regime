<?= $this->extend('layout/header') ?>
<?= $this->section('content') ?>

<div class="container mt-4">
    <h1>📊 Tableau de bord Admin</h1>
    <p>Bienvenue, <?= session()->get('admin_nom') ?></p>

    <!-- Cartes statistiques -->
    <div class="row mb-4">
        <div class="col-md-3">
            <div class="card text-white bg-success mb-3">
                <div class="card-body">
                    <h5><?= $totalUsers ?></h5>
                    <p>Utilisateurs</p>
                </div>
            </div>
        </div>
        <div class="col-md-3">
            <div class="card text-white bg-warning mb-3">
                <div class="card-body">
                    <h5><?= $totalGold ?></h5>
                    <p>Membres Gold</p>
                </div>
            </div>
        </div>
        <div class="col-md-3">
            <div class="card text-white bg-info mb-3">
                <div class="card-body">
                    <h5><?= $usersWithObjectif ?></h5>
                    <p>Objectifs actifs</p>
                </div>
            </div>
        </div>
    </div>

    <!-- Graphiques -->
    <div class="row">
        <div class="col-md-6">
            <canvas id="objectifChart"></canvas>
        </div>
        <div class="col-md-6">
            <canvas id="imcChart"></canvas>
        </div>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
<script>
    new Chart(document.getElementById('objectifChart'), {
        type: 'bar',
        data: {
            labels: ['Perdre', 'Prendre', 'IMC idéal'],
            datasets: [{
                label: 'Objectifs en cours',
                data: [<?= $objectifStats['reduire_poids'] ?>, <?= $objectifStats['augmenter_poids'] ?>, <?= $objectifStats['imc_ideal'] ?>],
                backgroundColor: ['#dc3545', '#28a745', '#ffc107']
            }]
        }
    });

    new Chart(document.getElementById('imcChart'), {
        type: 'pie',
        data: {
            labels: ['Normal', 'Surpoids', 'Obèse'],
            datasets: [{
                data: [<?= $imcStats['normal'] ?>, <?= $imcStats['surpoids'] ?>, <?= $imcStats['obese'] ?>],
                backgroundColor: ['#28a745', '#ffc107', '#dc3545']
            }]
        }
    });
</script>

<?= $this->endSection() ?>
<?= $this->include('layout/footer') ?>